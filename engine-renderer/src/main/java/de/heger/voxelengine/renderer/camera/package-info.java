/**
 * The player's eye on the world.
 *
 * <p>{@link de.heger.voxelengine.renderer.camera.Camera} is a first-person camera:
 * it holds a position and a look direction (as yaw and pitch), turns keyboard and
 * mouse input into movement and rotation, and produces the view and projection
 * matrices the renderer needs. Field of view, view distance, and aspect ratio all
 * live here, so anything about "what the player sees and from where" starts in
 * this package.</p>
 */
package de.heger.voxelengine.renderer.camera;
