/**
 * Visual aids for development and for the player's targeting reticle.
 *
 * <p>{@link de.heger.voxelengine.renderer.debug.BlockOutlineRenderer} draws the
 * highlight around the block the player is looking at, and
 * {@link de.heger.voxelengine.renderer.debug.WireframeRenderer} can outline
 * geometry for debugging chunk boundaries and mesh issues. These are lightweight,
 * self-contained draws layered on top of the main scene.</p>
 */
package de.heger.voxelengine.renderer.debug;
