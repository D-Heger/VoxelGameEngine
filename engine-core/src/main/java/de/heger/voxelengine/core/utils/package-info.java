/**
 * Odds and ends that do not belong to any single subsystem.
 *
 * <p>{@link de.heger.voxelengine.core.utils.Validate} offers small argument-checking
 * helpers so methods can fail fast with clear messages, and
 * {@link de.heger.voxelengine.core.utils.PerformanceMonitor} tracks frame timing
 * and memory use for the debug overlay. If something is broadly useful and has
 * no better home, it tends to land here.</p>
 */
package de.heger.voxelengine.core.utils;
